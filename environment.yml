name: geneset-extractor
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python
  - r-essentials
  - bioconductor-limma
  - bioconductor-edger
prefix: /path/to/miniconda3/envs/geneset-extractor
