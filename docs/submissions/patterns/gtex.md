# GTEx pattern

Choose `gtex` for tissue-partitioned bulk RNA-seq models sharing inputs and
having multiple analytical model families. Keep tissue/model enumeration in
config and dispatch the existing or new DIG workflow from thin wrappers.
