# Generic pattern

Choose `generic` only when no reference pattern fits. Document the closest
reasonable comparison and every deviation. It does not relax the DIG ownership,
input-manifest, reproduction, model-sidecar, metadata, provenance, or output
requirements.
