# HuBMAP pattern

Choose `hubmap` for a released-reference library construction workflow, such as
ASCT+B-derived inputs. Use explicit source releases and a small set of
library-construction model paths that all emit the shared DIG output contract.
