# Commands Used For GTEx Step 2

The step-2 generator was originally created against legacy `M*` model IDs. The current GTEx-local naming uses `AB*` for the same age-binned models.

```bash
python3 geneset-extractor-dev/GTEx/src/generate_gtex_model_assets.py
```
