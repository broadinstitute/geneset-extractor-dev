# run_gtex_model_sweep.v1

Shell wrapper for `src/run_gtex_model_sweep.v1.py`.

This wrapper generates the step-2 run scripts and provenance files for `GTEx_model_sweep_v1`. It does not execute any model workflows itself.
